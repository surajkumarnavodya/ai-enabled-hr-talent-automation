# Evaluation Reports

> Owner: [TENANT_CONFIGURATION_REQUIRED — AI Governance Lead] | Status: Draft

Holds completed evaluation run outputs and scorecards (see [ai-evaluation-scorecard.md](../../docs/09-quality-evaluation/ai-evaluation-scorecard.md)), referenced from the `evaluation_record` table ([data-dictionary.md](../../docs/03-data/data-dictionary.md)).

Reports may contain sensitive evaluation detail (e.g., fairness test results) — see `.gitignore` rules excluding `*.local.json`/`*.tmp` working files. Finalized reports checked into version control must not contain real candidate/employee data.

No reports exist yet in this documentation-only phase.
